#include <iostream>
#include <Windows.h>
#include "Klasy.h"
#include <conio.h>
#include <thread>
int main(int argc, char* argv[]) {
    std::vector<blok> bloki;
    gra Breakout;
    if (true) {
        Breakout.menu(Breakout.poziom_nr, Breakout);
    }
        return 0;
    }